@model IEnumerable<TradeCompany.Models.History>

@{
    ViewData["Title"] = "Index";
}

<h1 style="text-align: center; font-size: 3rem; margin-bottom: 4px;">History</h1>

<div class="row" style="text-align: center;">
    <div class="col-md-4" style="text-align: center;">
        <form asp-action="DownloadExcelReport" method="get">
            <div class="form-group">
                Choose Id history
                <select name="id" asp-items="ViewBag.His"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Download" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<p style="text-align: center;">
    <a style="text-align: center; margin-bottom: 4px;" asp-action="Create">Create New</a>
</p>
<table class="table" style="text-align: center;">
    <thead style="text-align: center;">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShopId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody style="text-align: center;">
@foreach (var item in Model) {
        <tr style="text-align: center;">
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @($"{item.Shop.Name} - {item.Shop.Address}")
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
                @Html.ActionLink("Delete", "Delete", new {  id=item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
